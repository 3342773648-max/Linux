# B1 示例 Inventory（RFC 5737 文档地址）
# ⚠️ 仅用于本地实验，不得用于真实生产环境。
# 真实环境的 host_vars 应由用户自行创建，不入库（见 .gitignore 说明）。

all:
  children:
    k8s_cluster:
      children:
        control_plane:
          hosts:
            cp-01:
        worker:
          hosts:
            worker-01:
    etcd:                       # B1：stacked etcd（继承 control_plane），不做外部 etcd
      children:
        control_plane:
